meta {
  name: Approve Payment
  type: http
  seq: 3
}

docs {
  อนุมัติสลิป → เปลี่ยน payment → APPROVED, bill → PAID, ส่ง LINE แจ้ง user
}

patch {
  url: {{base_url}}/payments/{{payment_id}}/approve
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
